import React from 'react';
import { Fade } from 'react-reveal';

const Skillset = () => (
  <div className="skillset-container">
    <Fade left>
      <div className="skillset-side-note">SKILLSET</div>
    </Fade>
  </div>
);

export default Skillset;
